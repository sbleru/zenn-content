---
title: "DAMPなテスト戦略を模索する"
emoji: "😎"
type: "tech" # tech: 技術記事 / idea: アイデア
topics: []
published: false
---

# 何？

- テスト戦略を模索している
- テスト戦略を模索するために、DAMPなテスト戦略を模索する

## DAMPなテスト戦略を模索する

```ts
  it.skip('hoge', () => {
    // pass
    expect({
      a: 'a',
      b: 'b',
    }).toMatchObject({
      a: 'a',
      b: 'b',
    })

    // pass
    expect({
      a: 'a',
      b: 'b',
    }).toMatchObject({
      a: 'a',
    })

    // fail
    // expect({
    //   a: 'a',
    //   b: 'b',
    // }).toMatchObject({
    //   a: 'a',
    //   b: 'b',
    //   c: 'c',
    // })

    // fail
    // expect({
    //   a: 'a',
    //   b: 'b',
    // }).toMatchObject({
    //   a: 'a',
    //   b: 'c',
    // })

    // pass
    // aはあることをチェックしつつ、aの中身を厳密にチェックできない
    expect({
      a: {
        b: 'b',
        c: 'c',
      },
      z: 'z',
    }).toMatchObject({
      a: {
        b: 'b',
      },
    })

    // fail
    // aがあることをチェックしつつ、さらに中身を厳密にチェックできる
    // expect({
    //   a: {
    //     b: 'b',
    //     c: 'c',
    //   },
    //   z: 'z',
    // }).toMatchObject(
    //   expect.objectContaining({
    //     a: {
    //       b: 'b',
    //     },
    //   }),
    // )

    // pass
    expect({
      a: {
        b: 'b',
        c: 'c',
      },
      z: 'z',
    }).toMatchObject(
      expect.objectContaining({
        a: {
          b: 'b',
          c: 'c',
        },
      }),
    )

    // pass
    expect({
      a: {
        b: 'b',
        c: 'c',
      },
    }).toEqual({
      a: {
        b: 'b',
        c: 'c',
      },
    })

    // fail
    // expect({
    //   a: {
    //     b: 'b',
    //     c: 'c',
    //   },
    //   z: 'z',
    // }).toEqual({
    //   a: {
    //     b: 'b',
    //     c: 'c',
    //   },
    // })

    // pass
    expect({
      a: {
        b: 'b',
        c: 'c',
      },
      z: 'z',
    }).toMatchObject(
      expect.objectContaining({
        a: {
          b: 'b',
          c: 'c',
        },
      }),
    )

    // pass
    expect({
      a: {
        b: 'b',
        c: 'c',
      },
      z: 'z',
    }).toEqual(
      expect.objectContaining({
        a: {
          b: 'b',
          c: 'c',
        },
      }),
    )

    // // fail
    // expect({
    //   a: {
    //     b: 'b',
    //     c: 'c',
    //   },
    // }).toEqual({
    //   a: {
    //     b: 'b',
    //   },
    // })

    // // fail
    // expect({
    //   a: {
    //     b: 'b',
    //     c: 'c',
    //   },
    // }).toEqual({
    //   a: {
    //     b: 'b',
    //     c: 'c',
    //     d: 'd',
    //   },
    // })
  })
```